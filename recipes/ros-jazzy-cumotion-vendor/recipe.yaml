schema_version: 1

# ros-jazzy-cumotion-vendor, built FROM SOURCE against RoboStack.
#
# Generated by scripts/gen_source.py -- edit the generator, not this file.
# Source: isaac_ros_nitros/cumotion_vendor
# Detected build traits: cuda.

context:
  version: "5.0.0"

package:
  name: ros-jazzy-cumotion-vendor
  version: ${{ version }}

source:
  - url: https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_nitros/archive/refs/tags/v5.0-0.tar.gz
    sha256: dfba350fcaecf100b9d01d174c3acc8c5757d44ef4726d4bccbef721c117c4e9
    target_directory: src
  - url: https://media.githubusercontent.com/media/NVIDIA-ISAAC-ROS/isaac_ros_nitros/260cee67e8344db363f7584256ae161d03abfd01/cumotion_vendor/aarch64_jetpack70/lib/libcumotion.so.1.1.0
    sha256: 6977efcb757839a3d992c368d8bb9ef12c4ee728469b567ae3baa26d89717c76
    target_directory: src/cumotion_vendor/aarch64_jetpack70/lib
  - url: https://media.githubusercontent.com/media/NVIDIA-ISAAC-ROS/isaac_ros_nitros/260cee67e8344db363f7584256ae161d03abfd01/cumotion_vendor/aarch64_jetpack70/python_wheels/cumotion-1.1.0-cp312-cp312-linux_aarch64.whl
    sha256: 60f6aa4703e201ab9c1b5dd4f78d85c1aa5b1c1dfc83b785f85f66309f766359
    target_directory: src/cumotion_vendor/aarch64_jetpack70/python_wheels
  - url: https://media.githubusercontent.com/media/NVIDIA-ISAAC-ROS/isaac_ros_nitros/260cee67e8344db363f7584256ae161d03abfd01/cumotion_vendor/aarch64_jetpack70/python_wheels/cumotion_vis-1.1.0-py3-none-any.whl
    sha256: 0dce7c072d865d71fbb2ed44381d812d2601e836d82276341af7d2c39eaf24f2
    target_directory: src/cumotion_vendor/aarch64_jetpack70/python_wheels
  - url: https://media.githubusercontent.com/media/NVIDIA-ISAAC-ROS/isaac_ros_nitros/260cee67e8344db363f7584256ae161d03abfd01/cumotion_vendor/x86_64_cuda_13_0/lib/libcumotion.so.1.1.0
    sha256: fe13f8b2c1b6d908a72f087041a2385a1b4c2157323623bdf3e905f2852c9a2c
    target_directory: src/cumotion_vendor/x86_64_cuda_13_0/lib
  - url: https://media.githubusercontent.com/media/NVIDIA-ISAAC-ROS/isaac_ros_nitros/260cee67e8344db363f7584256ae161d03abfd01/cumotion_vendor/x86_64_cuda_13_0/python_wheels/cumotion-1.1.0-cp312-cp312-linux_x86_64.whl
    sha256: b3f3eb0699686f4c3b1ba218513040fe6cdcdf0e474f78d5a8fe06371819fec5
    target_directory: src/cumotion_vendor/x86_64_cuda_13_0/python_wheels
  - url: https://media.githubusercontent.com/media/NVIDIA-ISAAC-ROS/isaac_ros_nitros/260cee67e8344db363f7584256ae161d03abfd01/cumotion_vendor/x86_64_cuda_13_0/python_wheels/cumotion_vis-1.1.0-py3-none-any.whl
    sha256: ffebfff502ff599aa917503e2648255def6b815bf1092405bf732bb9bd056f3a
    target_directory: src/cumotion_vendor/x86_64_cuda_13_0/python_wheels

build:
  number: 0
  script:
    - export AMENT_PREFIX_PATH="${PREFIX}${AMENT_PREFIX_PATH:+:${AMENT_PREFIX_PATH}}"
    - export CMAKE_PREFIX_PATH="${PREFIX}${CMAKE_PREFIX_PATH:+:${CMAKE_PREFIX_PATH}}"
    - cd src/cumotion_vendor
    - test "$(grep -rl 'find_dependency(Eigen3 3.3)' -- */lib/cmake/cumotion/cumotionConfig.cmake | wc -l)" -eq 2
    - sed -i 's/find_dependency(Eigen3 3.3)/find_dependency(Eigen3)/' */lib/cmake/cumotion/cumotionConfig.cmake
    # ${CMAKE_ARGS} carries the compiler activation's CMAKE_FIND_ROOT_PATH, which is
    # what points find_package(CUDAToolkit) at the prefix rather than /usr/local/cuda.
    - >
      cmake -S . -B build -G Ninja ${CMAKE_ARGS:-}
      -DCMAKE_BUILD_TYPE=Release
      -DCMAKE_INSTALL_PREFIX="${PREFIX}"
      -DCMAKE_PREFIX_PATH="${PREFIX}"
      -DCMAKE_CUDA_ARCHITECTURES="${CUDA_ARCHITECTURES:-$(case "${target_platform:-$(uname -m)}" in linux-aarch64|aarch64) echo "87;110;120";; *) echo "80;86;89;90";; esac)}"
      -DPYTHON_EXECUTABLE="${PREFIX}/bin/python"
      -DBUILD_TESTING=OFF
    - cmake --build build --parallel "${CPU_COUNT:-2}"
    - cmake --install build
    # No installed *text* file may name the build environment. $BUILD_PREFIX is unique to
    # this build and is deleted afterwards, and conda's relocation does not rewrite it --
    # only $PREFIX is a placeholder -- so such a reference breaks every consumer.
    #
    # Not hypothetical: nvblox_ros_common passes CUDAToolkit to
    # ament_target_dependencies(), which makes the toolkit's include directory INTERFACE, and
    # FindCUDAToolkit resolves it next to nvcc -- in BUILD_PREFIX. It went into the installed
    # export file, the package built and tested clean, and the failure surfaced only when
    # nvblox_ros used it: "includes non-existent path".
    #
    # Rewrite first: the CUDA headers exist at the same relative paths under both prefixes
    # (the compile line carries -I for both), so substituting the host prefix is correct
    # rather than merely quieting the check. Then fail on anything left.
    #
    # Text files only. Binaries legitimately still carry BUILD_PREFIX in their RPATH at this
    # point -- rattler-build's post-processing relocates them after this script runs -- so
    # including them would fail every package for a non-problem.
    - >
      find "${PREFIX}" -type f \( -name '*.cmake' -o -name '*.pc' -o -name '*.sh'
      -o -name '*.dsv' -o -name '*.txt' -o -name '*.bash' -o -name '*.zsh' \)
      -exec grep -lF "${BUILD_PREFIX}" {} + 2>/dev/null
      | xargs -r sed -i "s|${BUILD_PREFIX}|${PREFIX}|g"
    - >
      left=$(find "${PREFIX}" -type f \( -name '*.cmake' -o -name '*.pc' -o -name '*.sh'
      -o -name '*.dsv' -o -name '*.txt' -o -name '*.bash' -o -name '*.zsh' \)
      -exec grep -lF "${BUILD_PREFIX}" {} + 2>/dev/null | head -5);
      if [ -n "$left" ]; then echo "FAIL: installed text files still name BUILD_PREFIX:";
      echo "$left"; exit 1; fi
  dynamic_linking:
    missing_dso_allowlist:
      # GXF extensions live in sibling packages' share/ trees; cuVSLAM and the VPI
      # backends are dlopen'd or resolved through the ament index.
      - libgxf_*.so
      - libcuvslam.so
      - libcumotion.so*
      - libnvvpi.so.*

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ compiler('cxx') }}
    - ${{ compiler('cuda') }}
    - cmake
    - ninja
    - pkg-config
  host:
    - eigen
    - ros-jazzy-ament-cmake
    - cuda-version 13.*
    - cuda-cudart-dev
    - cuda-nvtx-dev
  run:
    - __glibc >=2.38
    # none

tests:
  - package_contents:
      files:
        - share/cumotion_vendor/package.xml

about:
  homepage: https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_nitros
  summary: "Vendor package for the precompiled NVIDIA cuMotion SDK."
  license: LicenseRef-NVIDIA-Isaac-ROS
