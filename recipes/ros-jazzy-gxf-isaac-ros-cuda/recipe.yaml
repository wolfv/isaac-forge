schema_version: 1

# Repacked from the official Isaac ROS debian package.
#
# Generated by scripts/gen_repack.py -- edit the generator, not this file.
#
# The binaries are NVIDIA's own, unmodified except for RUNPATH rewriting, which is
# required to make them relocatable inside a conda prefix (see relink.py).

context:
  version: "4.5.0"

package:
  name: ros-jazzy-gxf-isaac-ros-cuda
  version: ${{ version }}

source:
  # ros-jazzy-gxf-isaac-ros-cuda
  - url: https://isaac.download.nvidia.com/isaac-ros/release-4/pool/main/r/ros-jazzy-gxf-isaac-ros-cuda/ros-jazzy-gxf-isaac-ros-cuda_4.5.0-0noble.20260706195015722_amd64.deb
    file_name: pkg0.deb
    sha256: adedc11bee70b872dad98b990954d3bbb7a6ab5a16dc01f8aab20e8aa9a97e99

build:
  number: 0
  script: build.sh
  dynamic_linking:
    # NVIDIA's binaries are relinked by relink.py, not by rattler-build.
    binary_relocation: false
    missing_dso_allowlist:
      - libcuda.so.*
      - libnvidia-*.so.*
      - libgxf_*.so
      - libcuvslam.so
      - libcumotion.so*
      - libnvvpi.so.*
      - libcvcuda.so.*
      - libnvcv_types.so.*
      - libopencv_*.so.406*

requirements:
  build:
    - libarchive          # bsdtar, reads .deb directly
    - patchelf
    - python
  run:
    # Built on Ubuntu 24.04, so the binaries reference GLIBC_2.38 symbols. This is
    # a hard requirement on the host glibc, hence a run dep rather than a
    # run_constraints entry.
    - __glibc >=2.38
    - ros-jazzy-gxf-isaac-cuda
    - ros-jazzy-isaac-ros-common
    - ros-jazzy-isaac-ros-gxf

tests:
  - package_contents:
      files:
        # ROS dirs use underscores, conda package names use dashes.
        - share/gxf_isaac_ros_cuda/package.xml

about:
  homepage: https://github.com/NVIDIA-ISAAC-ROS
  summary: GXF extension for Cuda related components in Isaac.
  license: LicenseRef-NVIDIA-Isaac-ROS
