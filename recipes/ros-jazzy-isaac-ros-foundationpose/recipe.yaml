schema_version: 1

# ros-jazzy-isaac-ros-foundationpose, built FROM SOURCE against RoboStack.
#
# Generated by scripts/gen_source.py -- edit the generator, not this file.
# Source: isaac_ros_pose_estimation/isaac_ros_foundationpose
# Detected build traits: ament_auto, cuda, cvcuda, eigen, magicenum, opencv, python, rosidl.

context:
  version: "4.5.0"

package:
  name: ros-jazzy-isaac-ros-foundationpose
  version: ${{ version }}

source:
  - url: https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_pose_estimation/archive/refs/tags/v4.5-0.tar.gz
    sha256: 7e6855053fbc1e9af5683c36e6e5642e2d5db9de8783753371ddeb4c565aee13
    target_directory: src

build:
  number: 0
  script:
    - export AMENT_PREFIX_PATH="${PREFIX}${AMENT_PREFIX_PATH:+:${AMENT_PREFIX_PATH}}"
    - export CMAKE_PREFIX_PATH="${PREFIX}${CMAKE_PREFIX_PATH:+:${CMAKE_PREFIX_PATH}}"
    - export CXXFLAGS="${CXXFLAGS:-} -I${PREFIX}/include/magic_enum"
    - export CUDAFLAGS="${CUDAFLAGS:-} -I${PREFIX}/include/magic_enum"
    - cd src/isaac_ros_foundationpose
    # ${CMAKE_ARGS} carries the compiler activation's CMAKE_FIND_ROOT_PATH, which is
    # what points find_package(CUDAToolkit) at the prefix rather than /usr/local/cuda.
    - >
      cmake -S . -B build -G Ninja ${CMAKE_ARGS:-}
      -DCMAKE_BUILD_TYPE=Release
      -DCMAKE_INSTALL_PREFIX="${PREFIX}"
      -DCMAKE_PREFIX_PATH="${PREFIX}"
      -DCMAKE_CUDA_ARCHITECTURES="80;86;89;90"
      -DPYTHON_EXECUTABLE="${PREFIX}/bin/python"
      -DBUILD_TESTING=OFF
    - cmake --build build --parallel "${CPU_COUNT:-2}"
    - cmake --install build
  dynamic_linking:
    missing_dso_allowlist:
      # GXF extensions live in sibling packages' share/ trees; cuVSLAM and the VPI
      # backends are dlopen'd or resolved through the ament index.
      - libgxf_*.so
      - libcuvslam.so
      - libcumotion.so*
      - libnvvpi.so.*

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ compiler('cxx') }}
    - ${{ compiler('cuda') }}
    - cmake
    - ninja
    - pkg-config
  host:
    - assimp
    - eigen
    - ros-jazzy-ament-cmake
    - ros-jazzy-ament-index-cpp
    - ros-jazzy-cv-bridge
    - ros-jazzy-geometry-msgs
    - ros-jazzy-isaac-ros-common
    - ros-jazzy-isaac-ros-dnn-image-encoder
    - ros-jazzy-isaac-ros-foundationpose-models-install
    - ros-jazzy-isaac-ros-image-proc
    - ros-jazzy-isaac-ros-nitros
    - ros-jazzy-isaac-ros-nitros-camera-info-type
    - ros-jazzy-isaac-ros-nitros-image-type
    - ros-jazzy-isaac-ros-nitros-tensor-list-type
    - ros-jazzy-isaac-ros-tensor-list-interfaces
    - ros-jazzy-isaac-ros-tensor-proc
    - ros-jazzy-message-filters
    - ros-jazzy-rcl-yaml-param-parser
    - ros-jazzy-rclcpp
    - ros-jazzy-rosidl-default-generators
    - ros-jazzy-rosidl-default-runtime
    - ros-jazzy-sensor-msgs
    - ros-jazzy-std-srvs
    - ros-jazzy-tf2-eigen
    - ros-jazzy-tf2-ros
    - ros-jazzy-vision-msgs
    - ros-jazzy-ament-cmake-auto
    - cuda-version 13.*
    - cuda-cudart-dev
    - cuda-nvtx-dev
    - libcvcuda >=0.16
    - libcvcuda-dev >=0.16
    - magic_enum
    - libopencv 4.13.*
    - python
    - pyyaml
  run:
    - __glibc >=2.38
    - assimp
    - ros-jazzy-ament-index-cpp
    - ros-jazzy-cv-bridge
    - ros-jazzy-geometry-msgs
    - ros-jazzy-isaac-ros-common
    - ros-jazzy-isaac-ros-dnn-image-encoder
    - ros-jazzy-isaac-ros-foundationpose-models-install
    - ros-jazzy-isaac-ros-image-proc
    - ros-jazzy-isaac-ros-nitros
    - ros-jazzy-isaac-ros-nitros-camera-info-type
    - ros-jazzy-isaac-ros-nitros-image-type
    - ros-jazzy-isaac-ros-nitros-tensor-list-type
    - ros-jazzy-isaac-ros-tensor-list-interfaces
    - ros-jazzy-isaac-ros-tensor-proc
    - ros-jazzy-message-filters
    - ros-jazzy-rcl-yaml-param-parser
    - ros-jazzy-rclcpp
    - ros-jazzy-rosidl-default-runtime
    - ros-jazzy-sensor-msgs
    - ros-jazzy-std-srvs
    - ros-jazzy-tf2-eigen
    - ros-jazzy-tf2-ros
    - ros-jazzy-vision-msgs

tests:
  - package_contents:
      files:
        - share/isaac_ros_foundationpose/package.xml

about:
  homepage: https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_pose_estimation
  summary: "FoundationPose 6D pose estimation"
  license: Apache-2.0
