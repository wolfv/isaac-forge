schema_version: 1

# ros-jazzy-isaac-ros-mcap-lerobot-converter, built FROM SOURCE against RoboStack.
#
# Generated by scripts/gen_source.py -- edit the generator, not this file.
# Source: isaac_ros_data_tools/isaac_ros_mcap_lerobot_converter
# Build type: ament_python (setuptools, installed with pip).

context:
  version: "4.6.0"

package:
  name: ros-jazzy-isaac-ros-mcap-lerobot-converter
  version: ${{ version }}

source:
  - url: https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_data_tools/archive/refs/tags/v4.6-0.tar.gz
    sha256: 427dcae558578f043e080ccba7f984cca74a475187180044f4f6d2d92f586064
    target_directory: src

build:
  number: 0
  script:
    - export AMENT_PREFIX_PATH="${PREFIX}${AMENT_PREFIX_PATH:+:${AMENT_PREFIX_PATH}}"
    - cd src/isaac_ros_mcap_lerobot_converter
    - ${{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv

requirements:
  host:
    - python
    - pip
    - setuptools
  run:
    - python
    - av
    - numpy
    - rosbags

tests:
  - package_contents:
      files:
        - share/isaac_ros_mcap_lerobot_converter/package.xml
        - share/ament_index/resource_index/packages/isaac_ros_mcap_lerobot_converter
  - python:
      imports:
        - isaac_ros_mcap_lerobot_converter
      pip_check: false

about:
  homepage: https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_data_tools
  summary: "Converts MCAP rosbags directly into LeRobot datasets"
  license: LicenseRef-NVIDIA-Isaac-ROS
