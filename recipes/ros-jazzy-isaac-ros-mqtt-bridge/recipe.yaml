schema_version: 1

# ros-jazzy-isaac-ros-mqtt-bridge, built FROM SOURCE against RoboStack.
#
# Generated by scripts/gen_source.py -- edit the generator, not this file.
# Source: isaac_ros_cloud_control/isaac_ros_mqtt_bridge
# Build type: ament_python (setuptools, installed with pip).

context:
  version: "4.6.0"

package:
  name: ros-jazzy-isaac-ros-mqtt-bridge
  version: ${{ version }}

source:
  - url: https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_cloud_control/archive/refs/tags/v4.6-0.tar.gz
    sha256: 33c4894eed43ccd0d20c02fdb1d8abee65935ff80f53d0a258e0e711c4ee4aa8
    target_directory: src

build:
  number: 0
  script:
    - export AMENT_PREFIX_PATH="${PREFIX}${AMENT_PREFIX_PATH:+:${AMENT_PREFIX_PATH}}"
    - cd src/isaac_ros_mqtt_bridge
    - ${{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv

requirements:
  host:
    - python
    - pip
    - setuptools
    - pyyaml
    - ros-jazzy-ament-index-python
    - ros-jazzy-isaac-ros-common
  run:
    - python
    - paho-mqtt
    - ros-jazzy-isaac-ros-common
    - ros-jazzy-rosbridge-library
    - ros-jazzy-vda5050-msgs
    - ros-jazzy-rclpy
    - ros-jazzy-std-msgs

tests:
  - package_contents:
      files:
        - share/isaac_ros_mqtt_bridge/package.xml
        - share/ament_index/resource_index/packages/isaac_ros_mqtt_bridge
  - python:
      imports:
        - isaac_ros_mqtt_bridge
      pip_check: false

about:
  homepage: https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_cloud_control
  summary: "MQTT Bridge"
  license: Apache-2.0
