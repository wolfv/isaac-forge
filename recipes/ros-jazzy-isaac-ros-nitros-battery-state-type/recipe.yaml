schema_version: 1

# ros-jazzy-isaac-ros-nitros-battery-state-type, built FROM SOURCE against RoboStack.
#
# Generated by scripts/gen_source.py -- edit the generator, not this file.
# Source: isaac_ros_nitros/isaac_ros_nitros_type/isaac_ros_nitros_battery_state_type
# Detected build traits: ament_auto, cuda, eigen, eigenfloor, magicenum, python, yamlcpp.

context:
  version: "4.5.0"

package:
  name: ros-jazzy-isaac-ros-nitros-battery-state-type
  version: ${{ version }}

source:
  - url: https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_nitros/archive/refs/tags/v4.5-0.tar.gz
    sha256: a8814fd02843a1098d00173671f03115b3b3575aabb078be371a7f977ad1e5c2
    target_directory: src

build:
  number: 0
  script:
    - export AMENT_PREFIX_PATH="${PREFIX}${AMENT_PREFIX_PATH:+:${AMENT_PREFIX_PATH}}"
    - export CMAKE_PREFIX_PATH="${PREFIX}${CMAKE_PREFIX_PATH:+:${CMAKE_PREFIX_PATH}}"
    - export CXXFLAGS="${CXXFLAGS:-} -I${PREFIX}/include/magic_enum"
    - export CUDAFLAGS="${CUDAFLAGS:-} -I${PREFIX}/include/magic_enum"
    - cd src/isaac_ros_nitros_type/isaac_ros_nitros_battery_state_type
    # See the eigenfloor note in scripts/gen_source.py: a versioned
    # find_package(Eigen3 ...) in config mode rejects eigen 5 rather than
    # accepting it, and NO_MODULE rules out the module-mode workaround.
    - grep -qE 'find_package\(Eigen3 [0-9]' CMakeLists.txt || { echo 'versioned find_package(Eigen3) not found -- upstream changed shape, revisit gen_source.py'; exit 1; }
    - sed -i -E 's|find_package\(Eigen3 [0-9.]+|find_package(Eigen3|' CMakeLists.txt
    # ${CMAKE_ARGS} carries the compiler activation's CMAKE_FIND_ROOT_PATH, which is
    # what points find_package(CUDAToolkit) at the prefix rather than /usr/local/cuda.
    - >
      cmake -S . -B build -G Ninja ${CMAKE_ARGS:-}
      -DCMAKE_BUILD_TYPE=Release
      -DCMAKE_INSTALL_PREFIX="${PREFIX}"
      -DCMAKE_PREFIX_PATH="${PREFIX}"
      -DCMAKE_CUDA_ARCHITECTURES="80;86;89;90"
      -DPYTHON_EXECUTABLE="${PREFIX}/bin/python"
      -DBUILD_TESTING=OFF
    - cmake --build build --parallel "${CPU_COUNT:-2}"
    - cmake --install build
  dynamic_linking:
    missing_dso_allowlist:
      # GXF extensions live in sibling packages' share/ trees; cuVSLAM and the VPI
      # backends are dlopen'd or resolved through the ament index.
      - libgxf_*.so
      - libcuvslam.so
      - libcumotion.so*
      - libnvvpi.so.*

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ compiler('cxx') }}
    - ${{ compiler('cuda') }}
    - cmake
    - ninja
    - pkg-config
  host:
    - ros-jazzy-ament-cmake
    - ros-jazzy-gxf-isaac-messages
    - ros-jazzy-isaac-ros-common
    - ros-jazzy-isaac-ros-nitros
    - ros-jazzy-isaac-ros-nova-interfaces
    - ros-jazzy-rclcpp
    - ros-jazzy-rclcpp-components
    - ros-jazzy-sensor-msgs
    - ros-jazzy-ament-cmake-auto
    - cuda-version 13.*
    - cuda-cudart-dev
    - cuda-nvtx-dev
    - eigen
    - magic_enum
    - python
    - pyyaml
    - yaml-cpp
  run:
    - __glibc >=2.38
    - ros-jazzy-gxf-isaac-messages
    - ros-jazzy-isaac-ros-common
    - ros-jazzy-isaac-ros-nitros
    - ros-jazzy-isaac-ros-nova-interfaces
    - ros-jazzy-rclcpp
    - ros-jazzy-rclcpp-components
    - ros-jazzy-sensor-msgs

tests:
  - package_contents:
      files:
        - share/isaac_ros_nitros_battery_state_type/package.xml

about:
  homepage: https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_nitros
  summary: "Isaac ROS NITROS Battery State Type"
  license: LicenseRef-NVIDIA-Isaac-ROS
