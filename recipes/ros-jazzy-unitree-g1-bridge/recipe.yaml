schema_version: 1

# ros-jazzy-unitree-g1-bridge, built FROM SOURCE against RoboStack.
#
# Generated by scripts/gen_source.py -- edit the generator, not this file.
# Source: isaac_ros_robots/unitree_g1/unitree_g1_bridge
# Build type: ament_python (setuptools, installed with pip).

context:
  version: "4.6.0"

package:
  name: ros-jazzy-unitree-g1-bridge
  version: ${{ version }}

source:
  - url: https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_robots/archive/refs/tags/v4.6-0.tar.gz
    sha256: 24bc833c0ce574367a82d4b1aa9ddebabd3b65f221fd55666a80f8c97b6fe7d1
    target_directory: src
    patches:
      - patches/0001-match-package-version.patch

build:
  number: 0
  script:
    - export AMENT_PREFIX_PATH="${PREFIX}${AMENT_PREFIX_PATH:+:${AMENT_PREFIX_PATH}}"
    - cd src/unitree_g1/unitree_g1_bridge
    - ${{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv

requirements:
  host:
    - python
    - pip
    - setuptools
  run:
    - python
    - ros-jazzy-geometry-msgs
    - ros-jazzy-rclpy
    - ros-jazzy-ros2launch
    - ros-jazzy-std-srvs
    - ros-jazzy-unitree-api

tests:
  - package_contents:
      files:
        - share/unitree_g1_bridge/package.xml
        - share/ament_index/resource_index/packages/unitree_g1_bridge
  - python:
      imports:
        - unitree_g1_bridge
      pip_check: false

about:
  homepage: https://github.com/NVIDIA-ISAAC-ROS/isaac_ros_robots
  summary: "Lightweight ROS 2 bridge that converts geometry_msgs/Twist on /cmd_vel into unitree_api/msg/Request messages o"
  license: Apache-2.0
