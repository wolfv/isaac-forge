--- a/unitree_g1/unitree_g1_ros2_control/CMakeLists.txt
+++ a/unitree_g1/unitree_g1_ros2_control/CMakeLists.txt
@@ -33,10 +33,12 @@
     message(STATUS "Fetching unitree_sdk2 v${UNITREE_SDK2_VERSION} from GitHub...")
     include(FetchContent)
     fetchcontent_declare(unitree_sdk2
-      GIT_REPOSITORY https://github.com/unitreerobotics/unitree_sdk2
-      GIT_TAG ${UNITREE_SDK2_VERSION}
-      GIT_SHALLOW TRUE
+      SOURCE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/unitree_sdk2"
     )
+    set(UNITREE_SDK_LIB
+      "${CMAKE_CURRENT_SOURCE_DIR}/unitree_sdk2/lib/${CMAKE_SYSTEM_PROCESSOR}/libunitree_sdk2.a"
+      CACHE FILEPATH "Vendored Unitree SDK2 archive")
+    set(BUILD_EXAMPLES OFF CACHE BOOL "Do not build Unitree SDK examples")
     fetchcontent_makeavailable(unitree_sdk2)
   endif()
 endif()
